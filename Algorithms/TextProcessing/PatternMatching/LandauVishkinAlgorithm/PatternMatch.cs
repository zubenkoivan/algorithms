namespace Algorithms.TextProcessing.PatternMatching.LandauVishkinAlgorithm
{
    public class PatternMatch
    {
        public readonly int Index;
        public readonly int Length;

        public PatternMatch(int index, int length)
        {
            Index = index;
            Length = length;
        }
    }
}